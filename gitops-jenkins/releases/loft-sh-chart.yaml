---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: loft
  namespace: loft
spec:
  chart:
    repository: https://charts.devspace.sh/
    name: loft
    version: 0.0.1
  values:
    admin:
      username: admin
    certIssuer:
      create: "false"
    ingress:
      host: "loft.jenkins.oit.nd.edu"
      tls:
        secret: loft-cert
